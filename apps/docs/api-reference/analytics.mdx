---
title: "Analytics"
api: "GET /v1/analytics"
description: "Session totals and cache hit rate."
---

Requires a session. Returns the public session, `saved_pct`, and cache stats when FastAPI is the gateway.
