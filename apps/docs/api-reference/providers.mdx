---
title: "Provider catalog"
api: "GET /v1/providers"
description: "Known OpenAI-compatible base URLs."
---

Public. Each row is `id`, `label`, `base_url`, and the env var we read for a key.

Unknown hosts still work if you pass `base_url` on connect.
