---
title: "Savings portal"
description: "Account-level spend versus always-frontier."
---

`/portal` sums every routed completion on the account: routed spend, frontier baseline, routing savings, cache savings.

The console playground and the CLI both write receipts.

`GET /v1/savings` with a `pmz_live_` key returns the same totals.
